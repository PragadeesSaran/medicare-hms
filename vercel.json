{
  "version": 2,
  "buildCommand": "pnpm install && pnpm -r --if-present run build",
  "outputDirectory": "artifacts/medicare-hms/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.mjs" },
    { "source": "/:path*", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.mjs": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  }
}
